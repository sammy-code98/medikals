[build]
  command = "npm run build"
  publish="dist"
  base = ""
  # The base directory should be the path to the nested Vue project


[[redirects]]
  from = "/*"
  to = "/dist/index.html"
  status = 200
